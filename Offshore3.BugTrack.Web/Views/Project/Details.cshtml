@model Offshore3.BugTrack.ViewModels.DetailsViewModel


<div id="topArea">
    <a href="#" id="Settings">Settings</a>&nbsp;|&nbsp;<a href="#" id="Issues">Issues</a>
</div>
<div id="mainArea">
    @Html.Action("Settings","Project",new {projectId=Model.ProjectId});
</div>
<script>
    $('#Settings').bind('click', function () {
        $('#mainArea').load('@(Url.Action("Settings","Project",new {projectId=Model.ProjectId}))');
    });
    $('#Issues').bind('click', function () {
        $('#mainArea').load( '@(Url.Action("Issues","Bug",new {projectId=Model.ProjectId}))' );
    });
</script>
<style>
    #topArea {
        height: 20px;
        border-bottom: 1px solid grey;
        margin-bottom: 0;
    }
    #mainArea {
        margin-top: 0;
    }
</style>
